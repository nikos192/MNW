.DS_Store

# Customer form exports / internal PDFs — never commit accidentally
*.pdf
CUSTOMER-*
.vercel
.env*

# Cloud credential files — use environment secrets/ADC, never repository files
*.pem
*service-account*.json
*service_account*.json
*merchant-credentials*.json
*merchant_credentials*.json
